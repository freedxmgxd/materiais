# C++ API for materiais

This directory is intended to host a C++ API translation of the Python `materiais` library.

Goals:
- Translate Python functionality to C++ as closely as possible
- Export material definitions in OpenMC format instead of MCNP

Use this folder as the starting point for OpenMC material export integration.
